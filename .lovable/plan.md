Publicar a versão atual do site pra que o fix dos 6 Reels (autoplay via IntersectionObserver + botão Play fallback) fique no ar em `titan-content-craft.lovable.app`.

Nenhuma alteração de código — só deploy.